Brief summary: -

Page/PWA started out as a debugging tool, text at the top of the page remained from that.

It was originally only designed to be use as a android mobile share to target only. 
Only later realised that I could call the page with parameters.
Rather than the script that parse the information from bluesky share code.

Just added some stylying to the page for better desktop display.
